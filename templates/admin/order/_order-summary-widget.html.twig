    <div class="row mt-3">
        <div class="col-md-12 col-lg-7 ml-auto">
            <ul class="order-summary mb-0 list-unstyled">
                <li class="order-summary-item">
                    <span>{{ 'order.subtotal'|trans }}</span>
                    <span>{{ order.totalAfterSale|money }}</span>
                </li>
                <li class="order-summary-item">
                    <span>{{ 'order.shipping'|trans }}</span>
                    <span class="JS--summaryDeliveryFee">{{ order.shippingFeeToPay|money }}</span>
                </li>
{#                <li class="order-summary-item text-muted-extra">#}
{#                    <span>{{ 'order.discount'|trans }}</span>#}
{#                    <span>25%</span>#}
{#                </li>#}
                <li class="order-summary-item">
                    <span>{{ 'order.total-amount-to-pay'|trans }}</span>
                    <span class="order-summary-total">{{ order.totalAmountToPay|money }}</span>
                </li>
            </ul>
        </div>
    </div>