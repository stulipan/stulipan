<div class="row">
    <div class="col-lg-12 text-muted text-center mb-5">
        Jelentkezz be fiókodba, hogy tudd követni rendelésed állapotát, elmentett címre kérni a kiszállítást.
    </div>

    <div class="form-group col-lg-12">
        <label for="_username" class="sr-onlyX form-label">Email cím</label>
        <input type="text" id="username" name="_username" class="form-control" placeholder="" required {% if last_username is defined and empty %}autofocus{% endif %} value="{{ last_username is defined ? last_username : '' }}" />
    </div>

    <div class="form-group col-lg-12">
        <label for="inputPassword" class="sr-onlyX form-label">Jelszó</label>
        <div class="input-group">
            <input type="password" id="inputPassword" name="_password" class="form-control border-right-0X" placeholder="" required />
            {#<div class="input-group-append">#}
            {#<span class="input-group-text bg-white text-gray-500"><i class="fas fa-eye"></i></span>#}
            {#</div>#}
        </div>
    </div>

    <div class="form-group col-lg-12 mb-3 text-left">
        <div class="custom-control custom-checkbox text-muted">
            <input type="checkbox" id="checkbox-signin" name="_remember_me" class="custom-control-input" checked="checked">
            <label class="custom-control-label align-middle" for="checkbox-signin">Remember me</label>
        </div>
    </div>

    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
    <div class="form-group col-lg-12 mt-2 text-center">
        <button type="submit" value="Mentés" class="btn btn-primary" formnovalidate>Bejelentkezek </button>
    </div>
</div>