{% extends 'admin/template-editing.html.twig' %}

{% set activeMenu = 'settings' %}
{% set pageTitle = 'settings.notification.notifications'|trans %}
{% set breadcrumb = {
    0: {
        'url': path('settings-home'),
        'text': 'settings.settings'|trans,
    },
} %}

{% set cancelUrl = path('settings-home') %}

{% block bodyClass %} notifications notification-list{% endblock %}

{% block headerActionsWrapper %}
    <a href="{{ path('notification-edit') }}" class="btn btn-primary">{{ 'settings.notification.new-email-template'|trans }}</a>
    <a href="{{ path('collection-list') }}" class="btn btn-secondary ml-2">{{ 'settings.notification.customize-template'|trans }}</a>
{% endblock %}

{% block content %}

        <div class="row mt-3 mt-md-20px">
            <div class="col-md-4 mb-3">
                <div class="text-muted">
                    {{ 'settings.notification.notifications-description'|trans }}
                </div>
                <div class="d-flex mt-3">
                    <a href="#" class="btn btn-secondary mb-2">{{ 'settings.notification.customize-template'|trans }}</a>
                </div>
            </div>
            <div class="col-md-8">
                <div class="mb-20px">
                    <div class="">
                        <div class="row">
                            <div class="col-md-12 mb-3 correction-no-padding">
                                <div class="card">
                                    <div class="card-body">
                                        {% if notifications is defined and notifications is empty %}
                                            <div class="h-100 text-center p-4">
                                                <div class="h1 text-muted-extra">
                                                    <i class="fas fa-search"></i>
                                                </div>
                                                <div class="font-18 mt-3 mb-2">{{ 'settings.notification.email-template-not-found'|trans }}</div>
                                            </div>
                                        {% endif %}
                                        {% if notifications is defined and notifications is not empty %}
                                            <div class="list-group list-group-flush">
                                            {% for item in notifications %}
{#                                                <div class="col-md-12 mb-3 correction-no-padding">#}
{#                                                    <div class="card">#}
{#                                                        <div class="card-body">#}
{#                                                            <div class="row">#}
                                                                <div class="list-group-item">
                                                                    <div class="d-flex flex-column flex-md-row justify-content-start align-items-start">
                                                                        <div class="d-flex flex-column align-items-start">
                                                                            <div class="">
                                                                                <a href="{{ path('notification-edit', {'id': item.id}) }}" class="">{{ item.name }}</a>
                                                                            </div>
                                                                        </div>
                                                                        <div class="d-flex flex-column align-items-start ml-0 ml-md-3">
                                                                            rtalal
                                                                        </div>
                                                                    </div>
                                                                </div>
{#                                                            </div>#}
{#                                                        </div>#}
{#                                                    </div>#}
{#                                                </div>#}
                                            {% endfor %}
                                            </div>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}