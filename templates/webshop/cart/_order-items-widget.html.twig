    <div class="">
        <h6 class="checkout-block-title">{{ 'checkout.cart.products'|trans }}</h6>
    </div>

    <div id="products_in_basket" class="mt-3">
        <div class="">
            <div class="row d-flex align-items-center">
                <div class="col-12">
                    {% for item in checkout.items %}
                        {% include 'admin/order/_order-item-single-widget.html.twig' %}
                    {% endfor %}

                    <div class="cart-item">
                        <div class="row d-flex align-items-center text-center">
                            <div class="col-12">
                                {% include 'webshop/cart/_cart-message-widget.html.twig' with {'message': checkout.message, 'messageAuthor':  checkout.messageAuthor} %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>