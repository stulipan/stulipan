<div class="form-row">

    <div class="col-lg-4">
        <div class="row mb-3">
            <div class="col-lg-12">
                <div class="settings-item">
                    <div class="settings-item-body">
                        <div class="settings-icon">
                            <i class="fas fa-cog"></i>
                        </div>
                        <div class="settings-option">
                            <div>
                                <span class="text-primary">{{ 'settings.dashboard.general-settings-title'|trans }}</span>
                            </div>
                            <div>
                                {{ 'settings.dashboard.general-settings-description'|trans }}
                            </div>
                        </div>
                    </div>
                    <a href="{{ path('settings-configuration') }}" class="tile-link"></a>
                </div>
            </div>
        </div>
        {% if is_granted('ROLE_DELIVERY_SETTINGS') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
                                <i class="fas fa-shipping-fast"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">Szállítás és fizetés</span>
                                </div>
                                <div>
                                    Szállítás és fizetési módok beállítása.
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('shipping-list') }}" class="tile-link"></a>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>

    <div class="col-lg-4">
        {% if is_granted('ROLE_DELIVERY_SETTINGS') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
                                <i class="far fa-calendar-alt"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">Szállítási napok</span>
                                </div>
                                <div>
                                    Kiemelt szállítási napok.
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('occasion-list') }}" class="tile-link"></a>
                    </div>
                </div>
            </div>
        {% endif %}
        {% if is_granted('ROLE_DELIVERY_SETTINGS') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
                                <i class="far fa-clock"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">Szállítási idősávok</span>
                                </div>
                                <div>
                                    Szállítási idősávok definiálása.
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('delivery-date-type-list') }}" class="tile-link"></a>
                    </div>
                </div>
            </div>
        {% endif %}
        {% if is_granted('ROLE_DELIVERY_SETTINGS') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
                                <i class="fas fa-shipping-fast"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">Szállítási díjak</span>
                                </div>
                                <div>
                                    Rendelj szállítási díjakat az egyes területekhez.
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('geo-price') }}" class="tile-link"></a>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>

    <div class="col-lg-4">
        {% if is_granted('ROLE_MANAGE_VAT') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
                                <i class="fas fa-hand-holding-usd"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">ÁFA</span>
                                </div>
                                <div>
                                    ÁFA típusok.
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('vat-rate-list') }}" class="tile-link"></a>
                    </div>
                </div>
            </div>
        {% endif %}
        {% if is_granted('ROLE_MANAGE_VAT') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
                                <i class="fas fa-hand-holding-usd"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">ÁFA értékek</span>
                                </div>
                                <div>
                                    ÁFA értékek.
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('vat-value-list') }}" class="tile-link"></a>
                    </div>
                </div>
            </div>
        {% endif %}
        {% if is_granted('ROLE_MANAGE_TRANSLATION') %}
            <div class="row mb-3">
                <div class="col-lg-12">
                    <div class="settings-item">
                        <div class="settings-item-body">
                            <div class="settings-icon">
    {#                            <i class="fas fa-globe-americas"></i>#}
                                <i class="fas fa-language"></i>
                            </div>
                            <div class="settings-option">
                                <div>
                                    <span class="text-primary">{{ 'settings.dashboard.translation-title'|trans }}</span>
                                </div>
                                <div>
                                    {{ 'settings.dashboard.translation-description'|trans }}
                                </div>
                            </div>
                        </div>
                        <a href="{{ path('translation-edit-byCategory') }}" class="tile-link"></a>
                    </div>

                </div>
            </div>
        {% endif %}
    </div>


</div>