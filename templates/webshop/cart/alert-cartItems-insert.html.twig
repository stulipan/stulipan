{% block AZUZENET %}
    {% for msg in app.session.flashBag.get('items-missing') %}
        <div class="JS--alertMessage alert alert-danger mt-3" role="alert" data-alert-message>
            <i class="fas fa-exclamation-circle mr-1"></i>
            {{ include('webshop/cart/alert-msg.html.twig') }}
        </div>
    {% endfor %}
{% endblock %}