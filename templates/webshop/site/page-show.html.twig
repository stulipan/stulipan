{% extends 'webshop/site/template-general.html.twig' %}

{% set pageTitle = page.name %}
{% set breadcrumb = {
    0: {
        'url': path('homepage'),
        'text': 'generic.home'|trans,
    },
    1: {
        'url': path('site-page-show', { 'slug': page.slug }),
        'text': page.name,
    },
} %}

{% block content %}
    <div class="row">
        <div class="col-xl-7 col-lg-10 mx-auto">
            <div class="cms-page-title hero-content pb-5 text-center">
                <h1 class="mb-0">{{ page.name }}</h1>
            </div>
            <div class="cms-page-body">
                {{ page.content|raw }}
            </div>
        </div>
    </div>
{% endblock %}


{% block footerHighlights %}
{% endblock %}

