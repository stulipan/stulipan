{% extends 'admin/base_admin.html.twig' %}


{% block title %}
	{{ title }}
{% endblock %}


{% block stylesheets %}
    {{ parent() }}
{% endblock %}


{% block page_content %}
		
	{% block LISTA %}

        <!-- Modal -->
        <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <!-- insert plain title -->
                        {{ include('/admin/_title_insert.html.twig') }}
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        {{ render(controller('App\\Controller\\Boltzaras\\BoltzarasController::addBoltzarasForm')) }}
                    </div>
                    {#<div class="modal-footer">#}
                        {#<button type="button" class="btn btn-secondary" data-dismiss="modal">Mégse</button>#}
                        {#<button type="button" class="btn btn-primary">M</button>#}
                    {#</div>#}
                </div>
            </div>
        </div>

        <!-- page title -->
        {{ include('/admin/_title_widget.html.twig') }}

        <div class="row">
            <div class="col-xl-3 col-lg-6 col-6">
                <div class="card widget-flat mb-2 mb-lg-3">
                    <div class="card-body">
                        <div class="float-right">
                            <i class="fas fa-shopping-basket text-muted fa-lg"></i>
                        </div>
                        <h5 class="text-muted font-weight-normal mt-0">Készpénz</h5>
                        <h3 class="text-gray my-0">{{ keszpenz|number_format(0, ',', ' ') }}<span class="h5 font-weight-normal"> Ft</span></h3>
                        <span class="text-sm font-weight-normal">&nbsp;</span>
                    </div>
                </div>
            </div> 

            <div class="col-xl-3 col-lg-6 col-6">
                <div class="card widget-flat mb-2 mb-lg-3">
                    <div class="card-body">
                        <div class="float-right">
                            <i class="far fa-credit-card text-muted fa-lg"></i>
                        </div>
                        <h5 class="text-muted font-weight-normal mt-0">Bankkártya</h5>
                        <h3 class="text-gray my-0">{{ bankkartya|number_format(0, ',', ' ') }}<span class="h5 font-weight-normal"> Ft</span></h3>
                        <span class="text-sm font-weight-normal">&nbsp;</span>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-lg-6 col-6">
                <div class="card widget-flat bg-success text-white mb-2 mb-lg-3">
                    <div class="card-body">
                        <div class="float-right">
                            <i class="fas fa-dollar-sign text-white fa-lg"></i>
                        </div>
                        <h5 class="font-weight-normal mt-0">Bolt összesen</h5>
                        <h3 class="my-0">{{ (keszpenz + bankkartya)|number_format(0, ',', ' ') }}<span class="h5 font-weight-normal"> Ft</span></h3>
                        <span class="text-sm font-weight-normal">Mr. Feka: {{ (keszpenz + bankkartya - kassza)|number_format(0, ',', ' ') }} Ft</span>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-lg-6 col-6">
                <div class="card widget-flat bg-info text-white mb-2 mb-lg-3">
                    <div class="card-body">
                        <div class="float-right">
                            <i class="fas fa-shopping-cart"></i>
                        </div>
                        <h5 class="font-weight-normal mt-0">Webshop</h5>
                        <h3 class="my-0">{{ webshop|number_format(0, ',', ' ') }}<span class="h5 font-weight-normal"> Ft</span></h3>
                        <span class="text-sm font-weight-normal">*csak 2018 adatok</span>
                    </div>
                </div>
            </div>

            {#<div class="col-xl-12">#}
                {#Bolt + Webshop: {{ (keszpenz + bankkartya + webshop)|number_format(0, ',', ' ') }} Ft#}
            {#</div>#}

            {#<div class="col-xl-3 col-lg-6 col-6">#}
                {#<div class="card widget-flat mb-2 mb-lg-3">#}
                    {#<div class="card-body">#}
                        {#<div class="float-right">#}
                            {#<i class="fas fa-dollar-sign text-muted fa-lg"></i>#}
                        {#</div>#}
                        {#<h5 class="text-muted font-weight-normal mt-0">Összbevétel</h5>#}
                        {#<h3 class="text-gray font-weight-normal my-1">{{ (keszpenz + bankkartya)|number_format(0, ',', ' ') }}<span class="h5 font-weight-normal"> Ft</span></h3>#}
                    {#</div>#}
                {#</div>#}
            {#</div> #}



        </div>

        <!-- header of default page content widget -->
        {{ include('/admin/_header_of_default_page_content.html.twig') }}

        <div class="row mb-2X">
            <div class="col-lg-8">
                <div class="d-inline-block my-1 my-lg-0">

                    <!-- dateRange Widget -->
                    {{ include('/admin/dateRange-picker-widget.html.twig') }}
                </div>
                <div class="d-inline-block">
                    <a href="{{  path('boltzaras_list') }}" class="btn-link ml-1">Teljes lista</a>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="text-lg-right my-1 my-lg-0">
                    <a href="{{ path('boltzaras_new') }}" class="btn btn-success mb-2 mr-2"><i class="fas fa-plus-circle mr-1"></i> Új jelentés</a>
                    <a href="{{ path('boltzaras-webshop-edit') }}" class="btn btn-info mb-2"><i class="fas fa-shopping-cart mr-1"></i> Új web forgalom</a>
                    {#<button type="button" class="btn btn-info mb-2" data-toggle="modal" data-target="#exampleModal">#}
                        {#<i class="fas fa-plus mr-1"></i> Új ablak</button>#}
                </div>
            </div>
        </div>




        <div class="table-responsive mb-3">
            <table class="table table-sm table-centered table-striped table-hover mb-0" id="boltzaras-list">
                <thead class="thead-light">
				<tr>
                    <th scope="col">Dátum</th>
                    <th scope="col">Kassza</th>
				    <th scope="col">Bankkártya</th>
				    <th scope="col">Készpénz</th>
				    <th scope="col">Összbevétel</th>
                    <th scope="col">Mr. Feka</th>
				    {#<th scope="col">ID</th>#}
                    <th scope="col" class="text-center">Módosítás időpontja</th>
                    <th scope="col">Munkatárs</th>
                    <th scope="col"><i class="fas fa-ellipsis-h"></i></th>
				  </tr>
			  </thead>
			  <tbody>
				{% if jelentesek is defined %}
				{% for jelentes in jelentesek %}
				<tr>
                    <td class="align-middle font-weight-bold text-sm">{{ jelentes.idopont|date('M d, Y')}}</td>
				    <td class="align-middle text-right text-nowrap">{{ jelentes.kassza|number_format(0, ',', ' ') }}</td>
				    <td class="align-middle text-right text-nowrap">{{ jelentes.bankkartya|number_format(0, ',', ' ') }}</td>
				    <td class="align-middle text-right text-nowrap">{{ jelentes.keszpenz|number_format(0, ',', ' ') }}</td>
				    <td class="align-middle text-right font-weight-bold text-nowrap">{{ (jelentes.bankkartya + jelentes.keszpenz)|number_format(0, ',', ' ') }}</td>
                    <td class="align-middle text-right text-nowrap">{{ (jelentes.bankkartya + jelentes.keszpenz - jelentes.kassza)|number_format(0, ',', ' ') }}</td>
  				    {#<td class="align-middle text-muted"><small>{{ jelentes.id }}</small></td>#}
                    <td class="align-middle text-center text-muted"><small>{{ jelentes.modositasIdopontja|date('Y-m-d, H:i')}}</small></td>
                    <td class="text-muted"><small><img class="img-thumbnail rounded-circle mr-2" src="{{ asset('/uploads/images/termekek/' ~ jelentes.munkatars.avatar) }}" width="20px;" />{{ jelentes.munkatars }} </small></td>
                    <td>
                        {#{% if is_granted('ROLE_POWER_ADMIN') %}#}
                            <a href="{{ path('boltzaras_edit', {'id': jelentes.id}) }}" class="btn-smX">
                                <i class="fas fa-edit"></i></a>
                        {#{% endif %}#}
				  	</td>
				</tr>
				{% endfor %}
				{% else %}
				<tr>
			        <td><li><em>nincs user found</em></li></td>
			    </tr>
				{% endif %}
			  </tbody>
			</table>
		</div>
		<div class="pagerfanta">
                {% if paginator is defined %}
					{{ pagerfanta(paginator, 'twitter_bootstrap4') }}
                {% endif %}
		</div>

        <!-- footer of default page content widget -->
        {{ include('/admin/_footer_of_default_page_content.html.twig') }}

	{% endblock %}
{% endblock %}





{% block javascripts %}
    {{ parent() }}
    {#
     A dateRange-picker-widget.html.twig fajlban van hasznalva
 #}
    <script>
        $(function() {
            $('#JS--idosav input').daterangepicker({
                opens: 'right',
                autoApply: true,
                autoUpdateInput: false,
                locale: {
                    format: 'YYYY-MM-DD',
                    separator: ' / ',
                    applyLabel: 'Alkalmaz',
                    cancelLabel: 'Mégse',
                    daysOfWeek: [
                        'V',
                        'H',
                        'K',
                        'Sz',
                        'Cs',
                        'P',
                        'Sz'
                    ],
                    monthNames: [
                        'Január',
                        'Február',
                        'Március',
                        'Április',
                        'Május',
                        'Június',
                        'Július',
                        'Augusztus',
                        'Szeptember',
                        'Október',
                        'November',
                        'December'
                    ],
                    firstDay: 1,
                }
            });

            $('#JS--idosav input').on('apply.daterangepicker', function(ev, picker) {
                $(this).val(picker.startDate.format('YYYY-MM-DD') + ' / ' + picker.endDate.format('YYYY-MM-DD'));
            });

            $('#JS--idosav input').on('cancel.daterangepicker', function(ev, picker) {
                $(this).val('');
            });

        });
    </script>
{% endblock %}
        

