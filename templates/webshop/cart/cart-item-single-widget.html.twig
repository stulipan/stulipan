        <!-- Cart single item-->
        <div class="slide-checkout--item">
            <div class="d-flex align-items-center">
                <a href="{{ path('site-product-show', {'id': item.product.id}) }}">
                    <img src="{{ uploaded_asset(item.product.coverImage)|imagine_filter('product_thumbnail') }}" alt="{{ item.product.name}}" class="img-fluid navbar-cart-product-image">
                </a>
                <div class="d-flex w-100 justify-content-between">
                    <div class="pl-3">
                        <a href="{{ path('site-product-show', {'id': item.product.id}) }}" class="slide-checkout--item-link">{{ item.product.name }}</a>
                        <small class="d-block text-muted">Mennyiség: {{ item.quantity }} </small>
                        <strong class="d-block text-sm">{{ (item.price * item.quantity) |number_format(0, ',', ' ') }} Ft</strong>
                    </div>
                    <a href="#"><i class="fa fa-trash-o"></i></a>
                </div>
            </div>
        </div>