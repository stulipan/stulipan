    {% if categories is defined %}
    {% for category in categories %}
        <a href="{{ path('site-product-list', {'slug': category.slug})  }}" class="dropdown-item">{{ category.name }}</a>
        {% if not loop.last %}
            <div class="dropdown-divider"></div>
        {% endif %}
    {% endfor %}
    {% endif %}