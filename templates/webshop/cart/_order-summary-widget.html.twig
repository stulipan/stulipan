    <div class="row mt-3">
        <div class="col-lg-12">
            <ul class="order-summary mb-0 list-unstyled">
                <li class="order-summary-item">
                    <span>Részösszeg </span>
                    <span>{{ order.summary.totalBeforeDiscount|money }}</span>
                </li>
                <li class="order-summary-item">
                    <span>Szállítás</span>
                    <span class="JS--summaryDeliveryFee">{{ order.deliveryFee|money }}</span>
                </li>
                <li class="order-summary-item text-muted-extra">
                    <span>Kedvezmény</span>
                    <span>25%</span>
                </li>
                <li class="order-summary-item">
                    <span>Fizentendő {# totalAmountToPay #}</span>
{#                    <span class="order-summary-total">{{ order.summary.totalAmountToPay|money }}</span>#}
                    <span class="order-summary-total">{{ order.summary.totalAmountToPay|money }}</span>
                </li>
            </ul>
        </div>
    </div>