    {# az alabbi sorban a 'form-boltzaras' a signin.css-bol a class neve #}
    {{ form_start(form, { 'attr': {'class': '' }}) }}
    <div class="row">
        <div class="col-lg-6">

            <div class="mb-3">
                <div class="form-group row">
                    <div class="col-sm-4 text-sm-right">
                        {{ form_label(form.company) }}
                    </div>
                    <div class="col-sm-8">
                        {{ form_widget(form.company, {'attr': {
                            'class': '',
                        }}) }}
                        {{ form_errors(form.company) }}
                        <small class="form-text text-muted">
                            Add meg a beszállítócég nevét.
                        </small>
                    </div>
                </div>
            </div>

			{# Render CSRF token etc .#}
			<div style="display:none">
				{{ form_rest(form) }}
			</div>

            <div class="mb-3">
                <div class="form-group row">
                    <div class="col-sm-4 text-sm-right">
                    </div>
                    <div class="col-sm-8 ml-sm-auto">
                        <input type="submit" value="Mentés" class="btn btn-lgg btn-success" formnovalidate>
                        {#<a href="{{ path('invoice-list') }}" class="btn btn-outline-secondary">Mégse</a>#}
                    </div>
                </div>
            </div>

        </div>
    </div>

    {{ form_end(form) }}