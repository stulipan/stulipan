<div class="text-muted">
    {% if order.postedAt is not null %}
    <span class="mr-2">{{ order.postedAt|localizedDate }}, {{ order.postedAt|localizedTime }}</span>
    {% endif %}
    <span class="mr-2"><span class="text-muted">{{ 'order.order-id'|trans }}</span> {{ order.id }}</span>
</div>