{# admin/base_admin.html.twig #}
{% extends 'admin/base_admin.html.twig' %}

{% set activeMenu = 'settings' %}
{%- block title -%}
    Dashboard
{%- endblock -%}

{% block page_content %}
    <div class="row">
        <div class="col-lg-12 col-xl-10 mx-auto">
            <div class="row mt-3">
                <div class="col-lg-12">
                    <div class="page-title h3 d-inline mr-md-2 mr-1">{{ 'settings.settings'|trans }}</div>
                </div>
            </div>
            <div class="row mt-2">
                <div class="col-lg-12 correction-no-padding">
                    <div class="card mb-20px">
                        <div class="card-body">
                            {% include '/admin/settings/home-widget.html.twig' %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}