            {#{{ form_start(customerForm) }}#}
                    <div class="form-row">
                        <div class="form-group col-sm-6">
                            <label for="{{ customerForm.email.vars.full_name }}" class="form-label">{{ customerForm.email.vars.label }}
                            </label>
                            {{ form_widget(customerForm.email, {'attr': {'class': ''}}) }}
                            {{ form_errors(customerForm.email) }}
                        </div>
                        <div class="form-group col-sm-6">
                            <label for="{{ customerForm.phone.vars.full_name }}" class="form-label">{{ customerForm.phone.vars.label }}</label>
                            {{ form_widget(customerForm.phone, {'attr': {'class': 'form-controlX'}}) }}
                            {{ form_errors(customerForm.phone) }}
                        </div>
                        <div class="form-group col-sm-6">
                            <label for="{{ customerForm.lastname.vars.full_name }}" class="form-label">{{ customerForm.lastname.vars.label }}
                            </label>
                            {{ form_widget(customerForm.lastname, {'attr': {'class': ''}}) }}
                            {{ form_errors(customerForm.lastname) }}
                        </div>
                        <div class="form-group col-sm-6">
                            <label for="{{ customerForm.firstname.vars.full_name }}" class="form-label">{{ customerForm.firstname.vars.label }}
                            </label>
                            {{ form_widget(customerForm.firstname, {'attr': {'class': ''}}) }}
                            {{ form_errors(customerForm.firstname) }}
                        </div>
                        <div class="form-group col-lg-12">
                            <div class="custom-control custom-checkbox mb-2 text-sm text-muted">
                                <input id="{{ customerForm.optin.vars.id }}" type="checkbox" name="{{ customerForm.optin.vars.full_name }}" class="custom-control-input" {{ customerForm.optin.vars.checked == true ? 'checked=\"checked\"' : '' }}>
                                <label for="{{ customerForm.optin.vars.id }}" class="custom-control-label align-middle">{{  customerForm.optin.vars.label }}</label>
                                {{ form_errors(customerForm.optin) }}
                                {% do customerForm.optin.setRendered() %}
                            </div>
                        </div>
                        {# Render CSRF token etc .#}
                        {#<div style="display:none">#}
                            {#{{ form_rest(customerForm) }}#}
                        {#</div>#}

                    </div>
            {#{{ form_end(customerForm) }}#}
            {% do customerForm.setRendered() %}