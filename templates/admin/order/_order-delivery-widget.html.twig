
    <div class="h5">{{ 'order.delivery-date'|trans }}</div>
{#    {% if order.isDeliveryOverdue is defined and order.isDeliveryOverdue == true %}#}
{#        <div class="alert alert-danger mb-3"><i class="fas fa-exclamation-circle mr-1"></i> {{ 'order.delivery-date-overdue'|trans }}</div>#}
{#    {% endif %}#}

    <div class="mt-3">
    {% if order.deliveryDate is defined and order.deliveryDate is not null %}
        <div class="">{{ order.deliveryDate|localizedDate|lower }}</div>
        <div class="">{{ order.deliveryInterval is defined and order.deliveryInterval ? order.deliveryInterval ~ ' óra között': '' }}</div>
    {% else %}
        {% include('admin/_missing-data-insert.html.twig') %}
    {% endif %}
    </div>

    <div class="mt-3">
    {% if order.deliveryDate is defined and order.deliveryDate is not null %}
        <em class="text-muted">{{ order.shippingMethod.getName() }}</em>
    {% else %}
        {% include('admin/_missing-data-insert.html.twig') %}
    {% endif %}
    </div>