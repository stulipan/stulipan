		{# az alabbi sorban a 'form-boltzaras' a signin.css-bol a class neve #}
		{{ form_start(form, { 'attr': {'class': 'form-boltzaras' }}) }}

        <div class="mb-3">
            <div class="form-group row">
                <div class="col-sm-4 text-sm-righttt">
                    {{ form_label(form.termeknev) }}
                </div>
                <div class="col-sm-8">
                    {{ form_widget(form.termeknev) }}
                    {{ form_errors(form.termeknev) }}
                    <small class="form-text text-muted">
                        Add meg a termék megnevezését.
                    </small>
                </div>
            </div>
        </div>
        <div class="mb-3">
            <div class="form-group row">
                <div class="col-sm-4 text-sm-righttt">
                    {{ form_label(form.category, form.category, { 'label_attr': {'class': 'col-form-label' }}) }}
                </div>
                <div class="col-sm-8">
                    {{ form_widget(form.category) }}
                    {{ form_errors(form.category) }}
                </div>
            </div>
        </div>



			{# Render CSRF token etc .#}
			<div style="display:none">
				{{ form_rest(form) }}
			</div>

            <div class="mb-3">
                <div class="form-group row">
                    <div class="col-sm-4 text-sm-righttt">
                    </div>
                    <div class="col-sm-8 ml-sm-auto">
                        <input type="submit" value="Mentés" class="btn btn-lgg btn-success" formnovalidate>
                        <a href="{{ path('keszlet_list') }}" class="btn btn-outline-secondary">Mégse</a>
                    </div>
                </div>
            </div>



		{{ form_end(form) }}